# 上市公司知识库 — 深度分析报告

> 基于 设计思想.md + llm-wiki.md + 设计思想_审查意见.md 的交叉对照
> 结合对 48 个 Python 脚本、123 个 wiki 页面、7 个进化机制的深度审计
> 分析日期：2026-04-23

---

## 一、审查意见的盲区：它说得对但不够深

审查意见给出 6.75/10 的综合评分，提出行业蒸馏、问题驱动搜索、投资判断层三个 P0/P1 建议。这些方向没错，但审查意见犯了一个典型错误——**它在评估"功能清单的完成度"，而非"系统是否真的在按设计思想运转"**。

具体来说：

1. **审查意见说"行业蒸馏机制缺失"** → 但 `sector_distiller.py` 已经实现了。真正的问题是它没被接入 `scheduler.py` 的自动流水线。审查意见看到的是功能缺口，实际是**管线断裂**。

2. **审查意见说"需要投资判断层"** → 但 `investment_judgment.py` 已经存在（零LLM成本的正则提取）。真正的问题是它输出的 `投资估值.md` 只是原始数据表，没有估值分析、没有同行对比、没有决策建议。**功能存在但价值为零**。

3. **审查意见说"缺少人机协作审核"** → 但 `review_queue.py` 已经存在。真正的问题是 config.yaml 里 `review_queue: enabled: false`。**机制存在但被关闭了**。

**核心洞察：这个项目的主要问题不是"缺功能"，而是"已有功能没有被正确连接和运转"。**

---

## 二、五个审查意见完全没触及的深层结构性问题

### 问题 1：知识只积累不压缩——"时间线膨胀症"

llm-wiki.md 的核心理念是：wiki 是一个 **persistent, compounding artifact**（持久的、复利增长的制品）。但当前实现把 "compounding" 误解为 "accumulating"。

**证据**：
- 北方华创 wiki：2578 行，200+ 时间线条目，时间跨度 2013-2026
- 中微公司 wiki：1350 行，130+ 条目

这些文件已经**超出人类可读阈值**。一个股票分析师不可能在一个 2578 行的文件里找到关键判断。

**llm-wiki.md 说的是什么？** 它说 "The synthesis already reflects everything you've read"——合成应该反映所有已读内容。但当前系统只做"追加"，不做"合成压缩"。

**缺失的机制**：定期的"知识蒸馏"——把 200 个时间线条目压缩成：
- 5-10 个关键判断（如"国产替代率从 15% 提升到 35%"）
- 3 个核心矛盾（如"营收增长 vs 毛利率下滑"）
- 1 个投资论点（如"订单饱满但估值偏高"）

**类比**：人类记忆不是无限堆叠的。我们会遗忘细节、保留判断。知识库也需要这个机制。

### 问题 2：矛盾检测是个"1.37M 噪音发生器"

审查意见提到矛盾检测"做得好"，但实际数据是：**1,366,522 个潜在矛盾，0 个高置信度**。

这不是"做得好"，这是**系统性失败**。

**根因分析**：`contradiction_detector.py` 的数值矛盾检测是用正则提取所有数字，然后两两比较。对于一个有 123 个 wiki 页面、数千个时间线条目的知识库，数字对的组合数是 O(n²) 级别。130万不是异常，是**数学必然**。

**真正的问题**：财务数据天然存在大量"合法差异"——不同季度、不同口径（合并/母公司）、不同币种、不同会计准则。正则匹配无法区分这些语义差异。

**llm-wiki.md 怎么说的？** "noting where new data contradicts old claims"——矛盾检测应该是**语义级别的**，不是数字级别的。LLM client 里有 `detect_contradictions()` 方法，但主检测器根本没调用它。

### 问题 3：配置系统的"三权分立"导致无人负责

| 配置文件 | 职责 | 实际使用 |
|----------|------|---------|
| `config.yaml` | 运行配置 | scheduler 直接读 YAML |
| `graph.yaml` + `companies.yaml` + `sectors.yaml` | 业务数据 | graph.py 合并加载 |
| `config_rules.yaml` | 质量规则 | config_rules_loader.py 独立加载 |
| `CLAUDE.md` | LLM 行为规范 | 人类阅读，LLM 参考 |

`config.py` 提供了统一的 `Config` dataclass，但 **几乎没被使用**：
- `collect_news.py` 直接 `yaml.safe_load("config.yaml")`
- `scheduler.py` 直接读 YAML
- `ingest_v2.py` 硬编码 `model = "deepseek-v4-flash"`
- `batch_assessment.py` 硬编码 `max_tokens=2048`

**结果**：改 `config.yaml` 里的 LLM 配置不会影响实际运行的脚本。配置系统名存实亡。

### 问题 4：`auto_discover.py` 的发现质量为零

审查意见说"实体自动发现 ✅ 100%"，但实际的 `suggestions.json` 里全是：
- `"该指标侧面反映出一家公司"` (置信度 1.0)
- `"市净率是公司"`
- `"股份有限公司"`
- `"公开发行证券的公司"`

**零个有效的公司发现。零个有效的新主题发现。**

这是正则表达式从财务报告 boilerplate 里提取的碎片，不是公司名。NOISE 过滤列表不够用。

### 问题 5：scheduler 的流水线是断裂的

| 脚本 | 是否接入 scheduler | 状态 |
|------|-------------------|------|
| collect_news.py | ✅ | 正常 |
| ingest_v2.py | ✅ | 正常 |
| batch_assessment.py | ✅ | 正常 |
| sector_distiller.py | ✅ | 已接入但审查意见说"缺失" |
| investment_judgment.py | ✅ | 已接入但输出质量差 |
| cross_verify.py | ✅ | 已接入 |
| contradiction_detector.py | ✅ | 已接入但噪音爆炸 |
| **evolve_questions.py** | ❌ | **必须手动运行** |
| **quality_dashboard.py** | ❌ | **必须手动运行** |
| **lint.py** | ❌ | **scheduler 的 lint 映射到 detect，不是真正的 lint** |

进化三件套（问题演化 + 质量看板 + lint）都没有接入自动流水线。这意味着**知识库的自我感知系统是手动的**。

---

## 三、与 llm-wiki.md 理念的根本性偏离

### 偏离 1：Wiki 是"代码库"，但没有"重构"

llm-wiki.md 说："Obsidian is the IDE; the LLM is the programmer; **the wiki is the codebase.**"

好的代码库需要**重构**——不是不断加新代码，而是定期重组已有代码，使其更清晰、更有结构。

当前 wiki 只有"追加"操作，没有"重构"操作。2578 行的北方华创 wiki 就像一个从不重构的 10 万行函数。

### 偏离 2："Query 的答案可以写回 wiki"——完全缺失

llm-wiki.md 第39行说了一个关键洞察：
> "good answers can be filed back into the wiki as new pages. A comparison you asked for, an analysis, a connection you discovered — these are valuable and shouldn't disappear into chat history."

但当前系统完全没有这个机制。`query.py` 和 `search.py` 是只读的——用户提问，得到答案，然后答案就消失了。没有"把有价值的分析结果写回 wiki"的路径。

### 偏离 3："Schema 是 LLM 和人类共同进化的"

llm-wiki.md 说 schema（CLAUDE.md）应该 "co-evolve over time as you figure out what works for your domain"。

但当前 `CLAUDE.md` 是静态的。没有机制根据实际运行反馈来调整 schema。比如：
- 如果某个 prompt 产生的条目质量差，schema 应该调整
- 如果某个行业分类不合理，schema 应该演化
- 如果用户经常修改 LLM 的输出，说明 prompt 需要改进

---

## 四、对审查意见评分体系的质疑

审查意见的评分：

| 维度 | 审查评分 | 我的评分 | 差异原因 |
|------|---------|---------|---------|
| 数据存储架构 | 9 | 8 | 文件膨胀问题被忽略 |
| 数据采集能力 | 7 | 7 | 同意 |
| 数据整理质量 | 8 | 7 | 安集科技只有2019数据、AMD只有新闻碎片，质量不均 |
| 自动化程度 | 8 | 6 | 三个关键脚本未接入 scheduler |
| 自我进化能力 | 7 | 4 | auto_discover 输出垃圾、evolve 未接入、矛盾检测噪音 |
| 投资判断支持 | 4 | 3 | investment_judgment.py 存在但输出无分析价值 |
| 人机协作设计 | 5 | 4 | review_queue 存在但被禁用 |
| 质量度量体系 | 6 | 5 | dashboard 只读不触发动作 |
| **综合评分** | **6.75** | **5.5** | 功能存在 ≠ 功能有效 |

**核心分歧**：审查意见按"功能是否存在"打分，我按"功能是否有效运转"打分。

---

## 五、真正应该做的四件事（不是加功能，而是修管线）

### 第一优先：修好 scheduler 流水线（1天）

把 `evolve_questions.py`、`quality_dashboard.py`、`lint.py` 接入 scheduler。

这不是新功能——是让已有功能真正运转起来。

具体改动：
- `scheduler.py`：新增 `run_evolve()`、`run_dashboard()`、`run_lint()` 方法
- `config.yaml`：在 schedule 任务中添加 evolve/dashboard/lint 步骤
- 确保 lint.py 的输出能触发自动修复（至少修复 broken links）

### 第二优先：关闭噪音，打开信号（2天）

1. **contradiction_detector.py**：
   - 禁用 O(n²) 数值扫描（或把阈值从 30% 提到 80%）
   - 改用 `llm_client.detect_contradictions()` 做语义检测
   - 只对同一 entity 的近期条目做检测（限定范围）

2. **auto_discover.py**：
   - 重写公司名提取逻辑，用 LLM 替代正则（成本极低，1次API调用即可）
   - 增强 NOISE 过滤列表
   - 或者直接用 graph.py 的 companies 列表做反向匹配

3. **batch_assessment.py**：
   - 不只填充缺失的评估，也要更新 stale 的评估
   - 在 scheduler 中按 `last_updated` 排序，优先处理最旧的页面

### 第三优先：实现"知识压缩"机制（3天）

新增 `scripts/consolidate.py`：
- 当 wiki 页面超过 500 行时触发
- 用 LLM 把时间线条目压缩为"关键判断 + 核心矛盾 + 投资论点"
- 旧条目归档到 `companies/{name}/wiki/archive/` 子目录
- 保持 wiki 页面在人类可读范围内（目标 <300 行）
- 在 scheduler 中每周运行一次

### 第四优先：闭合查询回路（2天）

修改 `query.py`：
- 当用户提问时，如果答案涉及新的分析/判断/对比，自动生成 wiki 页面
- 标记为 `type: synthesis` 或 `type: comparison`
- 附带 `sources_count: 0`（来自查询而非采集）
- 在 index.md 中添加条目

---

## 六、对设计思想本身的评价

### 设计思想的先进性（保持）

1. "按主题分类的时间线" 优于 "按文档分类的文件夹" ✅ — 这是知识复用的基础
2. "问题清单驱动" 优于 "关键词驱动" ✅ — 问题是分析师的思维框架
3. "自我进化" 优于 "人工维护" ✅ — 自动化是可持续性的关键

### 设计思想的缺失

设计思想.md 缺少一个关键维度：**知识的生命周期管理**。

知识不是只进不出的。它需要：
- **产生**（采集 + 整理）← 已实现
- **验证**（多源交叉）← 部分实现
- **蒸馏**（压缩为判断）← 完全缺失
- **衰减**（标记过时、降权）← 部分实现
- **淘汰**（归档或删除）← 完全缺失

当前系统只实现了"产生"和部分"验证/衰减"，没有"蒸馏"和"淘汰"。这就是为什么 wiki 页面会膨胀到不可读的程度。

---

## 七、总结：项目的真正定位

这个项目不是"自我进化的知识库"，它是一个**"半自动的研究助理"**：

| 能力 | 状态 | 说明 |
|------|------|------|
| 自动采集新闻 | ✅ | Tavily API + 每日调度 |
| 自动整理到时间线 | ✅ | ingest_v2 + LLM |
| 自动生成评估 | ⚠️ | 只填充缺失的，不更新已有的 |
| 自动蒸馏行业洞察 | ⚠️ | sector_distiller 存在但效果未验证 |
| 自动检测问题 | ⚠️ | 矛盾检测噪音太大 |
| 自动修复问题 | ❌ | 检测到问题但不修复 |
| 自动发现新实体 | ❌ | 输出全是垃圾 |
| 自动演化问题 | ❌ | 未接入 scheduler |
| 自动压缩知识 | ❌ | 完全缺失 |
| 查询结果写回 wiki | ❌ | 完全缺失 |

要从"半自动研究助理"进化到"自我进化的知识库"，需要：
1. **修好已有管线**（接入 scheduler）
2. **关掉噪音**（修矛盾检测和实体发现）
3. **加一个压缩层**（知识蒸馏）
4. **闭合查询回路**（有价值的分析写回 wiki）

这四件事做完，项目的"自我进化"才算名副其实。

---

## 八、与审查意见的具体建议对照

| 审查意见建议 | 优先级 | 我的评价 | 我的建议 |
|-------------|--------|---------|---------|
| 行业蒸馏机制 | P0 | ❌ 已存在（sector_distiller.py），问题在未接入 scheduler | 改为"接入 scheduler 并验证效果" |
| 问题驱动搜索 | P0 | ✅ 同意，但实现方式不同 | 不改 collect_news.py，而是用 LLM 从问题生成搜索词 |
| 评估历史化 | P1 | ✅ 同意，batch_assessment.py 已有历史归档 | 确认历史表格格式统一 |
| 投资判断层 | P1 | ⚠️ investment_judgment.py 已存在但质量差 | 重写输出格式，增加估值分析 |
| 多源交叉验证 | P2 | ✅ cross_verify.py 已存在 | 接入 scheduler，优化聚类算法 |
| 审核队列 | P2 | ⚠️ review_queue.py 已存在但被禁用 | 先启用，再优化 |
| **（我的新增）修好 scheduler** | **P0** | 审查意见未提及 | 把 evolve/dashboard/lint 接入 |
| **（我的新增）关掉噪音** | **P0** | 审查意见未触及 | 重写矛盾检测和实体发现 |
| **（我的新增）知识压缩** | **P1** | 审查意见完全未提及 | 新增 consolidate.py |
| **（我的新增）查询回路** | **P2** | 审查意见完全未提及 | 修改 query.py 写回 wiki |
