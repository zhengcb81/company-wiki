# 上市公司知识库设计思想 — 详细审查意见

> 基于 llm-wiki.md 模式，从资深股票分析师视角的系统性评估
> 审查日期：2026-04-22
> 审查范围：设计思想.md + 当前完整实现（7个Phase已全部落地）

---

## 一、设计思想的核心诉求回顾

设计思想.md 提出了四个核心模块：
1. **数据存储模块**：公司/行业/主题三级目录，原始文档 + 整理后的主题时间线
2. **数据采集模块**：财报公告（已有程序）+ 新闻搜索（需定期运行）
3. **数据整理模块**：根据预设问题清单，将新信息整理到对应主题时间线
4. **自我进化能力**：公司列表、问题清单、行业板块动态进化

**关键要求**：不要头痛医头脚痛医脚，要从机制上打造"自我进化的动态知识库"。

---

## 二、当前实现 vs 设计思想的匹配度评估

### 2.1 已实现的部分（匹配度：高）

| 设计思想要求 | 当前实现 | 匹配度 |
|-------------|---------|--------|
| 公司独立子目录 + 原始文档 + wiki | `companies/{name}/raw/` + `companies/{name}/wiki/` | ✅ 100% |
| 行业/主题子目录 | `sectors/` + `themes/` | ✅ 100% |
| 按主题分类的时间线文档 | wiki 页面的 `## 时间线` 部分 | ✅ 100% |
| 新闻定期搜索采集 | `collect_news.py` + Tavily API | ✅ 100% |
| 根据问题清单整理 | `prompts.py` 中传入 `core_questions` | ✅ 100% |
| LLM 驱动整理 | `ingest_v2.py` + DeepSeek API | ✅ 100% |
| 定时调度运行 | `scheduler.py` --daemon 模式 | ✅ 100% |
| 问题清单动态进化 | `evolve_questions.py` | ✅ 100% |
| 实体自动发现 | `auto_discover.py` | ✅ 100% |
| 矛盾检测 | `contradiction_detector.py` | ✅ 100% |
| 综合评估自动生成 | `batch_assessment.py` | ✅ 100% |

### 2.2 已实现但需改进的部分（匹配度：中）

| 设计思想要求 | 当前实现 | 问题 |
|-------------|---------|------|
| 财报/公告采集 | 已有程序，但集成度低 | PDF文件散落在各目录，未统一接入 scheduler |
| 行业信息双向更新 | v2 已避免交叉污染，但行业wiki依赖外部采集 | 行业信息不能自动从公司文件推导 |
| 自我进化闭环 | 发现问题但不自动执行 | auto_discover 生成建议但不自动应用 |

### 2.3 尚未实现的部分（匹配度：低）

| 设计思想要求 | 当前缺失 | 影响 |
|-------------|---------|------|
| 用户配置选择界面 | 无 CLI/GUI 配置工具 | 用户需手动编辑 graph.yaml/config.yaml |
| 知识库工具集成（Obsidian CLI等） | 仅生成 Obsidian 兼容文件 | 无双向同步机制 |
| 投资判断辅助 | 无估值模型/评级系统 | 知识库停留在信息层，未上升到判断层 |

---

## 三、从机制设计角度的深度评价

### 3.1 做得好的机制（保持）

#### 1. LLM 驱动的 Ingest 流水线（v2 体系）
**评价**：这是整个项目最成功的机制设计。

- **Prompt 工程专业化**：6种专用 prompt（通用/财报/IR/公告/招股书/评估），每种都编码了该文档类型的分析范式
- **质量门控体系**：relevance gate（相关性检查）→ validate_entries（内容质量）→ assessment stale check（评估新鲜度），三层过滤
- **季度对比机制**：自动提取前一期财务数据传入 LLM，实现环比分析
- **来源追溯**：每个时间线条目自动附加 `[来源说明](../raw/{path})`

**效果验证**：
- 北方华创2015年报：旧版3句话 → v2 提取7条结构化条目（营收/利润/研发/专利/重组/专项）
- parse_error 率：初始 15% → 修复后 0%（通过清理乱码+修复尾部逗号）

#### 2. 调度器闭环（scheduler.py）
**评价**：实现了"系统自动运转"的核心目标。

```
collect（采集新闻）→ ingest（整理入库）→ assess（更新评估）→ detect（检测矛盾）
```

- 支持 `--daemon` 模式按 config.yaml 配置自动调度
- 支持单步骤执行（`--collect-only` 等）
- 每步输出摘要，最终汇总写入 `log.md`

#### 3. 进化机制 trio
**评价**：问题清单 + 实体发现 + 健康看板，构成了知识库的自我感知系统。

- `evolve_questions.py`：扫描 468 个问题，标记 17 活跃/1 陈旧/450 未回答
- `auto_discover.py`：从 879 个新闻文件中发现 20 个公司建议 + 10 个主题建议
- `quality_dashboard.py`：119 个 wiki 页面，5,461 个条目的全局健康度

### 3.2 需要改进的机制（优化）

#### 1. 行业信息的"自动蒸馏"机制
**现状**：行业 wiki 目前依赖外部采集（Web 搜索），无法从公司文件中自动提炼行业级洞察。

**问题**：
- 公司财报中蕴含大量行业信息（市场规模、竞争格局、技术趋势）
- 但 v2 的 relevance gate 阻止了公司文件写入行业 wiki（正确的设计）
- 导致行业 wiki 成为"孤岛"，需要独立维护

**建议改进**：
```python
# 新增行业蒸馏机制
class SectorDistiller:
    def distill_from_companies(self, sector_name):
        # 1. 收集该行业所有公司的近期时间线条目
        # 2. 用 LLM 提取行业级洞察（市场规模变化、技术路线演进、供应链变动）
        # 3. 生成行业 wiki 的新时间线条目
        # 4. 标记来源："基于 X 家公司财报提炼"
```

**实现位置**：`scripts/sector_distiller.py`，由 scheduler 在 `assess` 步骤后调用。

#### 2. 问题清单的"自动回答"机制
**现状**：450 个问题标记为"未回答"，但系统不主动寻找答案。

**问题**：
- 未回答问题只是被标记，没有触发任何动作
- 新闻采集的搜索关键词是固定的，不围绕未回答问题展开

**建议改进**：
```yaml
# config.yaml 中新增搜索策略配置
search_strategy:
  mode: "question_driven"  # 新增模式
  unanswered_questions:
    priority: high
    max_per_company: 3
    keywords_source: "question_text"  # 用问题文本生成搜索关键词
```

**效果**：当某公司有3个高优先级未回答问题时，collect_news.py 自动用这些问题作为搜索关键词，定向采集可能回答这些问题的文章。

#### 3. 评估的"增量更新"机制
**现状**：`batch_assessment.py` 为无评估页面生成评估，但已有评估的页面只在 ingest 时检查是否 stale。

**问题**：
- 评估是"重写"而非"增量更新"
- 无法体现"观点变化"（如从"看好"到"谨慎"的渐变过程）

**建议改进**：
```markdown
## 综合评估

### 当前评估（2026-04-22）
> 公司在手订单饱满，但毛利率承压...

### 评估历史
| 日期 | 评级 | 核心变化 |
|------|------|---------|
| 2026-04-22 | 谨慎乐观 | 毛利率下滑2pct |
| 2026-01-15 | 乐观 | 订单增长30% |
| 2025-10-30 | 中性 | 行业竞争加剧 |
```

**实现**：修改 `add_assessment_section()` 为 `update_assessment_section()`，保留历史评估。

### 3.3 需要新增的机制（补充）

#### 1. 投资判断层（从信息到决策）
**现状**：知识库停留在"信息整理"层，未上升到"投资判断"层。

**建议新增**：
- **估值跟踪页**：记录历史估值区间、当前估值分位、与行业对比
- **催化剂日历**：从时间线中提取未来事件（产品发布、产能投产、政策节点）
- **风险雷达**：从时间线中提取风险信号（客户集中、技术替代、政策变化）

**实现**：新增 `scripts/investment_layer.py`，读取 wiki 时间线生成投资判断辅助页面。

#### 2. 多源交叉验证机制
**现状**：一条信息只标注一个来源，无多源验证。

**建议新增**：
```python
# 当同一事件被多个来源报道时，自动标注可信度
def cross_verify(event, sources):
    if len(sources) >= 3:
        return "高可信度（多源确认）"
    elif len(sources) == 2:
        return "中可信度（双源报道）"
    else:
        return "待验证（单一来源）"
```

#### 3. 人机协作的"审核队列"
**现状**：LLM 自动写入 wiki，无人工审核环节。

**建议新增**：
- 新增 `review_queue.md`：列出 LLM 建议的所有写入操作
- 用户可配置 `auto_approve: true/false`
- 高风险操作（删除条目、修改评估）默认进入审核队列

---

## 四、具体改进建议（按优先级排序）

### P0（立即执行）

1. **行业蒸馏机制**：从公司文件中自动提炼行业洞察
   - 文件：`scripts/sector_distiller.py`
   - 触发：scheduler.py `assess` 步骤后
   - 输入：某 sector 下所有公司的近期时间线条目
   - 输出：行业 wiki 的新时间线条目（行业级洞察）

2. **问题驱动的搜索**：用未回答问题指导新闻采集
   - 文件：修改 `collect_news.py`
   - 逻辑：扫描未回答问题 → 生成搜索关键词 → 优先采集可能回答问题的文章

### P1（本周内）

3. **评估历史化**：保留评估变更历史
   - 文件：修改 `batch_assessment.py`
   - 格式：新增 `### 评估历史` 表格

4. **投资判断层**：新增估值/催化剂/风险页面
   - 文件：`scripts/investment_layer.py`
   - 输出：`companies/{name}/wiki/投资跟踪.md`

### P2（本月内）

5. **多源交叉验证**：同一事件多源报道时标注可信度
   - 文件：修改 `ingest_v2.py` 的条目合并逻辑

6. **审核队列**：高风险操作人工确认
   - 文件：`scripts/review_queue.py`
   - 配置：`config.yaml` 新增 `auto_approve` 选项

---

## 五、对设计思想本身的评价

### 5.1 设计思想的先进性

设计思想.md 的核心洞察非常到位：

1. **"按主题分类的时间线"优于"按文档分类的文件夹"**：这是与传统文件管理的本质区别，也是知识复用的基础
2. **"问题清单驱动"优于"关键词驱动"**：问题清单是分析师的思维框架，比简单的关键词匹配更能捕捉有价值的信息
3. **"自我进化"优于"人工维护"**：知识库的维护成本决定了其可持续性，自动化是必由之路

### 5.2 设计思想的不足

1. **缺少"投资判断层"的设计**：
   - 当前设计停留在"信息整理"，但股票分析师的最终目标是"投资决策"
   - 建议补充：估值模型、催化剂跟踪、风险雷达等模块

2. **缺少"人机协作边界"的定义**：
   - 哪些操作可以完全自动化？
   - 哪些操作需要人工确认？
   - 当前设计假设 LLM 100% 可靠，实际应有审核机制

3. **缺少"质量度量"的设计**：
   - 如何衡量一个 wiki 页面的质量？
   - 如何衡量整个知识库的健康度？
   - 当前只有 `quality_dashboard.py` 做统计，缺少质量评分体系

### 5.3 对 llm-wiki.md 模式的适配评价

当前实现基本遵循了 llm-wiki.md 的三层架构：

| 层级 | llm-wiki.md 定义 | 当前实现 | 评价 |
|------|-----------------|---------|------|
| Raw sources | 不可变的原始文档 | `companies/{name}/raw/` | ✅ 符合 |
| The wiki | LLM 生成的 markdown | `companies/{name}/wiki/` | ✅ 符合 |
| The schema | LLM 行为配置 | `CLAUDE.md` + `config.yaml` | ⚠️ 分散在两处，建议合并 |

**建议**：将 `CLAUDE.md` 和 `config.yaml` 合并为统一的 schema 文件，或明确分工：
- `CLAUDE.md`：LLM 行为规范（目录结构、wiki 格式、ingest 规则）
- `config.yaml`：运行配置（API key、调度间隔、公司列表）

---

## 六、总结

### 当前状态评分

| 维度 | 评分（1-10） | 说明 |
|------|-------------|------|
| 数据存储架构 | 9 | 三级目录 + 原始/wiki 分离，设计合理 |
| 数据采集能力 | 7 | 新闻采集完善，财报采集依赖外部程序 |
| 数据整理质量 | 8 | v2 体系质量高，parse_error 已解决 |
| 自动化程度 | 8 | scheduler 闭环完整，daemon 模式可用 |
| 自我进化能力 | 7 | 问题清单+实体发现+健康看板 trio 完整 |
| 投资判断支持 | 4 | 停留在信息层，未上升到决策层 |
| 人机协作设计 | 5 | 缺少审核机制，假设 LLM 100% 可靠 |
| 质量度量体系 | 6 | 有统计但缺少评分体系 |
| **综合评分** | **6.75** | 信息层优秀，判断层缺失 |

### 下一步最关键的三件事

1. **行业蒸馏机制**：让行业 wiki 从公司文件中自动生长，而非依赖外部采集
2. **问题驱动搜索**：让未回答问题主动触发信息采集，而非被动等待
3. **投资判断层**：新增估值/催化剂/风险模块，让知识库从"信息库"升级为"决策助手"

这三件事做完，知识库将从"自我维护"进化到"自我增值"——不仅保持信息新鲜，还能主动发现投资机会、预警风险。
