# 虚拟数据湖复杂度诊断（只读）

用户目标：同等授权的财报目录构成一个虚拟数据湖，用户不需要关心物理位置；检查当前实现和门禁膨胀原因，提出减法方案。本轮不实施、不改变权限、旧计划、数据、worker或任务。planning-with-files仅用于留下小型诊断记录，不新增大型实施DAG。

- [x] 核当前配置/索引模型/选择器/下游路径限制，分清已有虚拟化与泄漏；独立消费链探索补充7类证据。
- [x] 将门禁分成数据访问、外发/修改安全、开发验收流程，判断保留/合并/移除；95计划门不冒充每文件运行门。
- [x] README给出最小本地门面、四个增量与7类真实验收，不另建大型实施DAG，不实施或自动替代旧计划。

限制：只读源码/配置/Git；不扫描真实财报正文、不查询生产大库、不跑下载/LLM/worker。当前源码会并发变化，以实际观测时间/文件为准。
